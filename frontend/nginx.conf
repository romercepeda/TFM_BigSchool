server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — all unknown paths serve index.html so the History API router works.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
