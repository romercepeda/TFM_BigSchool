{
  "name": "bigschool-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "npm run validate:i18n",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "pretest": "npm run validate:i18n",
    "test": "vitest run",
    "test:watch": "vitest",
    "validate:i18n": "node scripts/validate-i18n.mjs"
  },
  "dependencies": {
    "@preact/signals-core": "^1.5.1"
  },
  "devDependencies": {
    "@open-wc/testing": "^4.0.0",
    "@types/node": "^20.0.0",
    "happy-dom": "^20.10.6",
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^1.6.0"
  }
}
