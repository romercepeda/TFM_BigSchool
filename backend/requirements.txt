# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.32.1

# ── Data validation & settings ────────────────────────────────────────────────
pydantic==2.10.3
pydantic-settings==2.7.0
email-validator==2.2.0   # enables pydantic.EmailStr

# ── Configuration file parsing ────────────────────────────────────────────────
pyyaml==6.0.2

# ── Authentication ────────────────────────────────────────────────────────────
PyJWT==2.9.0             # JWT token creation and validation
bcrypt==4.2.1            # password hashing (Spec 00b §2)

# ── Database — ORM + migrations ───────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.36
alembic==1.14.0

# ── Database — drivers ────────────────────────────────────────────────────────
asyncpg==0.30.0          # async driver used by the application at runtime
psycopg2-binary==2.9.10  # sync driver used only by Alembic for migrations

# ── HTTP client (for external API calls: market data, FX) ─────────────────────
httpx==0.28.1

# ── Async task queue (Spec D07) ───────────────────────────────────────────────
celery[redis]==5.4.0

# ── AI provider SDKs (Spec D07) ───────────────────────────────────────────────
anthropic==0.40.0         # Anthropic Claude (default provider)
openai==1.57.0            # OpenAI GPT (alternative provider)
google-genai==1.5.0       # Google Gemini (alternative provider)

# ── PDF text extraction (OpenAI provider — no native PDF document blocks) ─────
pypdf==4.3.1

# ── Multipart form support (required for FastAPI UploadFile) ──────────────────
python-multipart==0.0.20

# ── Development & testing ─────────────────────────────────────────────────────
ruff==0.8.4
pytest==8.3.4
pytest-cov==6.0.0
pytest-asyncio==0.24.0
